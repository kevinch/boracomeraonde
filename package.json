{
  "name": "eggReactApp",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^3.3.0",
    "react-scripts": "0.9.0"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-router": "^3.0.2"
  },
  "scripts": {
    "start": "concurrently --kill-others \"react-scripts start\" \"json-server -p 8080 --watch db.json\"",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
